{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Index</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.user_name }}</li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
{% endblock %}
